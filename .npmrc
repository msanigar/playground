# Use https instead of ssh for GitHub dependencies
# This resolves authentication issues in CI/CD environments
registry=https://registry.npmjs.org/
@whereby.com:registry=https://registry.npmjs.org/

# Ensure platform-specific binaries are installed
fund=false 